# 学习记录

## 第 1 周：模型 API 与流式聊天界面

### 2026-08-06

#### 已完成

- 明确第一个项目为 **Agent Chat Workspace**。
- 完成了最小聊天界面、流式消息渲染和 DeepSeek 模型调用路由。
- 已将代码提交为 `3e780d2 feat: 链接 deepseek 模型，发起 ai chat`。

#### 当前风险 / 阻塞

- 生产构建受当前环境无法下载 Google Fonts 影响而失败；后续需要在联网环境完成生产构建验证。

#### 今日完成清单（预计 3.5–4 小时）

- [x] **端到端验收（30 分钟）**：已完成正常提问、连续提问、空输入不请求、接口异常提示与重试入口 4 项测试。
- [x] **聊天体验（90 分钟）**：补齐页面标题和空状态、用户/助手消息气泡、发送中提示；发送空内容时不请求接口。
- [x] **失败可见性（45 分钟）**：将接口报错改成用户能理解的提示，并提供一次“重新发送”操作或明确的重试指引。
- [ ] **项目说明（30 分钟）**：更新 `agent-workspace/README.md`，写明启动方式、所需环境变量和当前能力边界；不要写入真实 API Key。
- [x] **复盘与提交（30 分钟）**：记录今天的学习结果；代码与学习记录已提交为 `19229d5`。

#### 今日完成定义

页面既能完成一次真实流式对话，也能让用户分辨“等待回复”和“请求失败”；另一位开发者仅按 README 就能配置并启动项目。

#### 验收结果

- 正常提问、连续提问：DeepSeek 能正常返回回复。
- 空输入：输入框为空或只含空格时，不产生 `/api/chat` 请求。
- 接口异常：保留用户消息，显示可读错误卡片与“重试”按钮。

#### 明日预告

在确认聊天链路稳定后，设计会话数据结构，并先用本地存储实现会话列表与恢复；随后再学习 FastAPI 和数据库持久化。

### 2026-08-07

#### 已完成

- 阅读 Vercel AI SDK 的消息持久化示例，理解 `chatId`、历史读取和流结束保存的职责。
- 创建 `/chat` 与 `/chat/[id]` 路由：新会话生成 `chatId` 并跳转；动态页面按 ID 读取历史消息。
- 新增 `chat-store.ts`，使用 `.chats/<chatId>.json` 保存每段会话。
- 在模型流结束的 `onEnd` 回调中保存完整消息；已验证刷新同一会话 URL 后可以恢复历史记录。
- 将 `.chats/` 加入 `.gitignore`，避免提交本地聊天数据。

#### 学到什么

- `useChat` 管理当前页面的即时状态；持久化层负责刷新后恢复数据。
- `chatId` 是会话的唯一标识，可映射到路由和存储文件。
- 选择在流结束后保存，能避免把不完整的流式回复反复写入存储。
- 本地 JSON 文件适合学习；真实部署需要替换为数据库或云存储。

#### 下一步

- 阅读并补齐项目 README：启动方式、环境变量和当前能力边界。
- 学习 FastAPI + SQLite，将 `loadChat` / `saveChat` 的文件实现替换为服务端 API 与数据库实现。
